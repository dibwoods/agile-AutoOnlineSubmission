# agile-AutoOnlineSubmission
Create a excel file (*.xls, *.xlsx)
Enter data to be submitted in Name, Email, Subject, Message order.
Run the application and select the excel file.

Uncomment #define HTML post statement at the top of Main.cs to use an alternative method which modifies and interacts with the webpage form directly.
